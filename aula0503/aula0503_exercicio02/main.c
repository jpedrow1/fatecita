#include <stdio.h>
#include <stdlib.h>
#define PI 3.14159
/*
Calculo do volume de uma esfera.
Fórmula abaixo:
V = π · r² · h
V = π * (r * r) * h
*/
int main()
{
    double volume, raio;
   // printf("Digite o valor do Raio:  \n");
   // scanf("%lf", &raio);
   // volume =  (double)4/(double)3 * PI * (raio*raio*raio);
   // printf("O volume dessa esfera: %lf \n", volume);
    int valor = 4;
    printf("%f  \n", (float)valor/3);
    return 0;
}
